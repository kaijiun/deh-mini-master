<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background"
    android:orientation="vertical">

    <TextView
        android:id="@+id/ans_record_title"
        android:layout_width="match_parent"
        android:layout_height="90dp"
        android:gravity="bottom"
        android:paddingLeft="@dimen/materialize_typography_body2"
        android:paddingBottom="@dimen/materialize_typography_body2"
        android:text="Answer history"
        android:textSize="30sp" />

    <android.support.v7.widget.RecyclerView
        android:id="@+id/answer_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_marginStart="0dp"
        android:layout_marginTop="90dp"
        android:layout_weight="9" />

    <com.getbase.floatingactionbutton.FloatingActionButton
        android:id="@+id/main_fab"
        android:layout_width="100dp"
        android:layout_height="112dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="25dp"
        android:clickable="true"
        app:fab_colorNormal="@color/blue"
        app:fab_colorPressed="@color/accent"
        app:fab_icon="@android:drawable/ic_menu_close_clear_cancel" />

</RelativeLayout>
